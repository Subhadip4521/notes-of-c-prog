//addition of two numbers
#include<stdio.h>

int main(){
    int num1, num2, add;
    printf("Enter two numbers: ");
    scanf("%d%d",&num1,&num2);
    add=num1+num2;
    printf("The addition of these two numbers are %d",add);
    return 0;
}