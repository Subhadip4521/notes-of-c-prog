
#include <stdio.h>

int main()
{
    // int a=2;
    // int *ptr=&a;
    // printf("The address of a is %u\n",ptr);
    // ptr++;
    // printf("The address of a is %u\n",ptr);
    
    // float a=3.2;
    // float *ptr=&a;
    // printf("The address of a is %u\n",ptr);
    // ptr++;
    // printf("The address of a is %u\n",ptr);
    
    // char a='2';
    // char *ptr=&a;
    // printf("The address of a is %u\n",ptr);
    // ptr++;
    // printf("The address of a is %u\n",ptr);
    
    return 0;
}
