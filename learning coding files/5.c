//check if the number given by user divisible by 97 or not//

#include<stdio.h>

int main()
{
    int num;
    printf("\n enter the number ");
    scanf("%d", &num);
    printf("\n divisibility test of the number %d",num%97);
    return 0;
}