
#include <stdio.h>

int main()
{
    int *ptr;
    
    //size of memory allocation in c
    printf("The size of int in my pc is %d\n",sizeof(int));
    printf("The size of float in my pc is %d\n",sizeof(float));
    printf("The size of char in my pc is %d\n",sizeof(char));

    return 0;
}
