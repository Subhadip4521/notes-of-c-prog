//various types of declaration of string

#include<stdio.h>
#include<string.h>

int main(){
    // char name[]={'s','u','b','h','a','\0'};          1st way to declare

    // char name[]="subha";                             2nd way to declare

    // char *name="subha";                              3rd way to declare
    
    // char name[10];
    // scanf("%s",name);
    // scanf("%[^\n]",name);                               this type of specification is known as edit set conversion code
    // printf("%s",name);                              

    // char name;
    // name= getchar();
    // putchar(name);                                   way of getting and putting a single character

    // char name[10];
    // gets(name);
    // puts(name);                                         using gets and puts

    return 0;
}